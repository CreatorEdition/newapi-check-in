# API 优先签到策略

## 目标

让已有 session cookies + api_user 的账号优先直接调用 NewAPI 接口；仅在 API 失败且账号存在邮箱密码凭据时，才使用浏览器登录/WAF Cookie 作为恢复路径，降低 Cloudflare/验证码对主流程的影响。

## 约束

- 不执行真实登录、签到或通知请求。
- 保持旧油猴脚本的令牌优先语义。
- API 失败分类必须可观测，不能把登录失败误报为签到成功。
- 修改必须同步 README、task.md、离线测试，并提交中文 commit。
